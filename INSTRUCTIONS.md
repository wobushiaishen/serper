运行所需最小说明

1) 在 `fastapi.claude.py` 顶部填写必需项：
   - `LLM_API_KEY`（LLM API Key）
   - `LLM_BASE_URL`（LLM 平台 URL）
   - `LLM_MODEL`（模型名）
   - `阿里云搜索MCP的密钥（MCP apikey）

 
   测试账号：用户名 `测试员`，密码 `hongyan`。（唯一一个能进去的账户）

